# Core ML stack — A1, A2, A6, A7, A8, A9, A10. Installs in seconds, CPU-only.
numpy>=1.26,<2.1
scipy>=1.11
scikit-learn>=1.4
xgboost>=2.0
PyWavelets>=1.5
joblib>=1.3
shap>=0.45
matplotlib>=3.8
pandas>=2.1
Pillow>=10.0
tqdm>=4.66
# optional: only needed if a live MQTT broker is running
paho-mqtt>=1.6
# real-dataset fetchers (A1 RF + A4 vision via Kaggle API)
kaggle>=1.6
